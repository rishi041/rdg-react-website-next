@AGENTS.md

# Project guide for AI agents

Read `README.md` first — it documents the product (OnGoodPicks board + portfolio), the
feature → file map, the AI contract ("DB first, AI once, keep the last good data", model
fallback, date anchoring), the data model and the project gotchas. `SETUP.md` covers cloud
Supabase / Gemini / Vercel. Keep README.md updated when you add or change a feature.
